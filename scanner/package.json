{
  "name": "@approval-sentinel/scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only ERC-20 approval scanner with risk scoring (ApprovalSentinel)",
  "type": "module",
  "scripts": {
    "scan": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
